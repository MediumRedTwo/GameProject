#include "includes.h"
using namespace std;

extern string Name;
extern string Race;
extern string WeaponType;      // what the name of the weapon they're using is
extern string ArmorType;      // what the name of the armor they're using is
extern string EnemyName; //  name of whoever you're fighting so it can easily jump functions
extern string EnemyFirstName; // Such as the, john, whatever using to identify the enemy (a, the, steve, etc)
extern string EnemyWeapon; // what the enemy attack is, such as sword, bite, claws, horns, etc
extern string Skill1;
extern string Skill2;
extern string Skill3;
extern string Skill4;
extern string Skill5;
extern int Acc0;
extern int Acc1;
extern int Acc2;
extern int Acc3;
extern int Acc4;
extern int Acc5;
extern int Acc6 ;
extern int Acc7;
extern int Acc8;
extern int Acc9;
extern int RaceType;
extern int HP;
extern int HPCurrent;
extern int Strength;
extern int Defense;
extern int Dodge;
extern int Gold;
extern int Potions;
extern int MegaPotions;
extern int WeaponAttack;
extern int ArmorNumber;    // all armor have a number assigned to them for determining if the new weapons are better or worse
extern int ArmorDefense;
extern int WeaponNumber;  // all weapons have a number assigned to them for determining if new weapons are better or worse
extern int Exp;
extern int ExpNextLevel;
extern int Level;
extern int HungerMax;
extern int HungerCurrent;
extern int FoodOnHand;
extern int SP;
extern int SPCurrent;
extern int HPBonus;
extern int SPBonus;
extern int STRBonus;
extern int DEFBonus;
extern int ACCBonus;
extern int DodgeBonus;
extern int GoldBonus;
extern int ClassStrBonus;
extern int ClassDefBonus;
extern int ClassHPBonus;
extern int ClassGoldBonus;
extern int StatsLocation;      // 1 = stats checks at the camp, 2 = stats checked in arena
extern int EnemyHP;
extern int EnemyHPCurrent;
extern int EnemyStr;
extern int EnemyEXP;
extern int EnemyGold;
extern int EnemyLevelCap;
extern int EnemyMissChance;
extern int EnemyDodgeChance;
extern int PotionInt; // Checks for this when wrapping up drinking a potion, 1= in camp, 2 = in the arena
extern int SkillNumber; // this is the attack check to see which skill is used, 0 = normal attack, 1 = skill 1, 2 = sk2, 3 = sk4, 4 = sk5
extern int DazeTimer;
extern int BleedTimer;
extern int BleedOn; // bleed 0 = no bleeding, bleed 1 = bleeding
extern int HungerUse;
extern int GladiatorFight;
extern int GladiatorRank;
extern int StoryLocation; // 0 = haven't gotten to the first boss, 1 = so on so forth, finish
extern int StoryBattle;


